package by.dashkevichpavel.osteopath.features.settings.backup.restore

enum class BackupRestoreUIState {
    START,
    BACKUP_FILE_SET,
    RESTORE_IN_PROGRESS,
    RESTORE_COMPLETE_SUCCESS,
    RESTORE_COMPLETE_FAIL,
    FINISH
}