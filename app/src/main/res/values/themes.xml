<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.Osteopath" parent="Theme.MaterialComponents.Light">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/themeLightPrimaryColor</item>
        <item name="colorPrimaryVariant">@color/themeLightPrimaryLightColor</item>
        <item name="colorOnPrimary">@color/themeLightPrimaryTextColor</item>

        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/themeLightSecondaryColor</item>
        <item name="colorSecondaryVariant">@color/themeLightSecondaryLightColor</item>
        <item name="colorOnSecondary">@color/themeLightSecondaryTextColor</item>

        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>

        <!-- Customize your theme here. -->
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

</resources>